os: Visual Studio 2017 RC
clone_depth: 10
build_script:
  - ps: |
      .\build.ps1
      if ($lastexitcode -ne 0){ exit $lastexitcode }
  - bash build-rename.sh
test: off
artifacts:
  - path: bin\*.nupkg